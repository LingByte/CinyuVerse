# Component Template

- Props: `size` `variant` `status` `disabled` `loading`
- Emits: `update:modelValue` or semantic events
- Slots: default + named extension slots
- A11y: keyboard support + `aria-*` + focus-visible
- Tests: basic render, interaction, disabled/error states
